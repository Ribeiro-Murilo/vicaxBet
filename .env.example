# Copie para .env e ajuste
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=vicaxbet
JWT_SECRET=segredo-da-firma-nao-conta-pra-ninguem
PORT=3001
# Endereco do back que o navegador chama em prod (sem barra no fim). Em dev pode deixar vazio (usa o proxy do Vite). Ex: http://192.168.1.35:3001
VITE_API_URL=
